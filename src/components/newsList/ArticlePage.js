import React from 'react';

const ArticlePage = ({title, author, description, url, urlToImage}) => {

    return (
        <div className="container">

        </div>
    );
}

export default ArticlePage;